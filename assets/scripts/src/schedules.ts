const pageSchedules = document.querySelector("#schedules");

if (pageSchedules) {

    const btnCancel = pageSchedules.querySelector(".cancel") as HTMLButtonElement;

    const list =  pageSchedules.querySelector("ul") as HTMLUListElement;

    const listItem =  list.querySelector("li") as HTMLLIElement;
    
}